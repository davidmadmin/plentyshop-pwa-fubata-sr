# Screw Finder module instructions

Read the repository root `CLAUDE.md` and this module's `README.md` before changing this directory.

- Preserve the block name `ScrewFinder` and existing content keys.
- Put public types in the adjacent `types.ts` file.
- Keep matching and safety logic in pure TypeScript modules with deterministic tests.
- Keep the editor block self-registering through `defaults.ts`; do not edit core block catalogues.
- Treat A2/A4 outdoor and corrosive requirements as safety-critical behavior.
- Verify both beginner and professional paths plus stale-request handling after flow changes.
